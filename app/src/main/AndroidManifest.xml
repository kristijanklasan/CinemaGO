<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="android.unipu.cinema">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/movie"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PitanjaActivity"></activity>
        <activity android:name=".CijenaKarata" />
        <activity android:name=".RezervacijaDetalji" />
        <activity
            android:name=".RezervacijaSjedala"
            android:configChanges="orientation"
            android:screenOrientation="sensorLandscape"
            tools:ignore="LockedOrientationActivity" />
        <activity android:name=".RezervacijaKarataActivity" />
        <activity android:name=".RezervacijeActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".TrailerPlayer"
            android:screenOrientation="sensorLandscape"
            tools:ignore="LockedOrientationActivity" />

        <activity android:name=".MoviesActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".MovieDetail" />
        <activity android:name=".startCinema" />
        <activity android:name=".registrationActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>